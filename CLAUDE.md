# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty aside from a README. Per the README, it is
a personal testing/playground repo intended to hold multiple unrelated
projects or experiments in one place, rather than a single cohesive
application.

There is no build system, linter, test suite, or source code yet, and
therefore no commands to document.

## Working here

- Do not assume a particular language, framework, or project layout —
  none has been established yet.
- Because this repo is meant to hold multiple, likely unrelated, things,
  expect future additions to live in their own subdirectories rather than
  being intermixed at the root. When adding a new project, give it its own
  top-level folder with its own build/test tooling rather than merging it
  into a shared structure.
- This file should be revisited and expanded once real code, tooling, or
  per-project conventions are added, so future sessions have accurate
  commands and architecture notes instead of this placeholder.
